class Node {
  constructor(value, next, prev) {
    this.value = values;
    this.next = next;
    this.prev = prev;
  }
}

class LinkedList {
  constructor() {
    this.head = null;
    this.tail = null;
  }

  addToHead(value) {
    let node = new Node(value, null, this.head);
    if (this.head) {
      //   this.head.next =
    }
  }
}
